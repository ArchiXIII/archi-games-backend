{
  "name": "archi-games-backend",
  "version": "1.5.0",
  "private": true,
  "description": "Archi Games serverless backend",
  "main": "index.js",
  "engines": {
    "node": ">=22.15.0"
  },
  "scripts": {
    "test": "node --test",
    "migrate": "node scripts/migrate.js",
    "build:zip": "node scripts/build-zip.js"
  },
  "dependencies": {
    "ydb-sdk": "^5.11.1"
  },
  "devDependencies": {
    "archiver": "^8.0.0"
  }
}
